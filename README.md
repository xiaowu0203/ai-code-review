<div align="center">

  # AI Code Review System

  **智能代码审核系统 | 基于 Gitee 的 AI 驱动代码审查工具**

  [![Java](https://img.shields.io/badge/Java-8-orange.svg)](https://www.oracle.com/java/)
  [![Spring Boot](https://img.shields.io/badge/Spring%20Boot-2.3.11-brightgreen.svg)](https://spring.io/projects/spring-boot)
  [![Vue.js](https://img.shields.io/badge/Vue.js-3.x-42b983.svg)](https://vuejs.org/)
  [![License](https://img.shields.io/badge/License-MIT-blue.svg)](LICENSE)

  集成多个 AI 大模型，为 Pull Request 提供自动化代码审核服务

  [快速开始](#-快速开始) • [功能特性](#-功能特性) • [部署指南](#-部署指南) • [API 文档](#-api-文档)

</div>

---

## 功能特性

| 特性 | 描述 |
|:---:|:---|
| 🤖 **多 AI 模型支持** | 集成千问、GLM、DeepSeek 等主流 AI 模型，可自由切换 |
| 🔄 **双触发模式** | 支持 Webhook 自动触发和手动触发审核 |
| 📊 **数据统计** | 提供审核数量、成功率、问题分布等多维度统计 |
| 🎯 **智能审核** | 从代码质量、性能、安全等多个维度进行审核 |
| 💾 **轻量部署** | 单容器部署，资源占用少，部署简单 |
| 🔒 **安全可靠** | 支持 Webhook Token 验证，保障接口安全 |

---

## 技术栈

### 后端
- **框架**: Spring Boot 2.3.11
- **数据库**: MySQL 5.7
- **ORM**: MyBatis Plus 3.4.2
- **HTTP**: OkHttp 4.9.1
- **工具**: Hutool 5.7.22

### 前端
- **框架**: Vue.js 3.x
- **UI**: Element Plus 2.x
- **图表**: ECharts 5.x
- **构建**: Vite

---

## 快速开始

### 前置要求

- Docker 20.10+
- Docker Compose 1.29+

### 一键启动

```bash
# 1. 克隆仓库
git clone https://github.com/your-username/ai-code-review.git
cd ai-code-review

# 2. 配置环境变量
cp .env.example .env
# 编辑 .env 文件，填写 API Keys

# 3. 启动服务
docker-compose up -d

# 4. 访问系统
open http://localhost:8080
```

### 本地开发

```bash
# 后端
cd backend
mvn spring-boot:run

# 前端（新终端）
cd frontend
npm install
npm run dev
```

---

## 部署指南

### 配置 AI 模型

复制 `.env.example` 为 `.env` 并填写配置：

```bash
# 千问（阿里云）
QIANWEN_API_KEY=your_qianwen_api_key

# GLM（智谱 AI）
GLM_API_KEY=your_glm_api_key

# DeepSeek
DEEPSEEK_API_KEY=your_deepseek_api_key

# Gitee Webhook
GITEE_WEBHOOK_TOKEN=your_webhook_token
```

### 配置 Gitee Webhook

1. 进入 Gitee 仓库 → 设置 → Webhooks
2. 添加新 Webhook：
   - URL: `http://your-domain/api/webhook/gitee`
   - 密钥: 与 `.env` 中 `GITEE_WEBHOOK_TOKEN` 保持一致
   - 事件: Pull Request

---

## 系统架构

```
┌─────────────────────────────────────────────────────────┐
│                     前端层                              │
│            Vue.js 3.x + Element Plus                   │
└─────────────────────┬───────────────────────────────────┘
                      │
┌─────────────────────▼───────────────────────────────────┐
│                    应用层                                │
│            Spring Boot 2.3.11                          │
│  ┌─────────────┬─────────────┬─────────────┐          │
│  │   项目管理   │   代码审核   │   Webhook   │          │
│  └─────────────┴─────────────┴─────────────┘          │
└─────────────────────┬───────────────────────────────────┘
                      │
┌─────────────────────▼───────────────────────────────────┐
│                    AI 服务层                             │
│  ┌─────────────┬─────────────┬─────────────┐          │
│  │    千问      │     GLM      │  DeepSeek   │          │
│  └─────────────┴─────────────┴─────────────┘          │
└─────────────────────┬───────────────────────────────────┘
                      │
┌─────────────────────▼───────────────────────────────────┐
│                    数据层                                │
│                 MySQL 5.7                              │
└─────────────────────────────────────────────────────────┘
```

---

## API 文档

### 项目管理

| 方法 | 路径 | 描述 |
|:---:|:-----|:-----|
| GET | `/api/projects` | 获取项目列表 |
| POST | `/api/projects` | 创建项目 |
| PUT | `/api/projects/{id}` | 更新项目 |
| DELETE | `/api/projects/{id}` | 删除项目 |

### 代码审核

| 方法 | 路径 | 描述 |
|:---:|:-----|:-----|
| POST | `/api/reviews/manual` | 手动触发审核 |
| GET | `/api/reviews/history` | 获取审核历史 |
| GET | `/api/reviews/{reviewId}` | 获取审核结果 |

### Webhook

| 方法 | 路径 | 描述 |
|:---:|:-----|:-----|
| POST | `/api/webhook/gitee` | Gitee Webhook 接口 |

---

## 使用指南

### 1. 添加项目

登录系统 → 项目管理 → 添加项目

填写以下信息：
- 项目名称
- Gitee 项目 ID
- 项目 URL
- Gitee 访问令牌
- 默认 AI 模型

### 2. 审核代码

**自动审核**：配置 Webhook 后，创建 PR 自动触发

**手动审核**：进入 PR 审核页面，选择项目和 PR ID，点击触发

### 3. 查看结果

审核完成后，结果会自动添加到 PR 评论中

---

## 目录结构

```
ai-code-review/
├── backend/              # Spring Boot 后端
│   ├── src/
│   │   └── main/
│   │       ├── java/
│   │       └── resources/
│   │           ├── application.yml
│   │           └── mapper/
│   └── pom.xml
├── frontend/             # Vue.js 前端
│   ├── src/
│   │   ├── components/
│   │   ├── views/
│   │   └── main.js
│   └── package.json
├── docker-compose.yml    # Docker 编排
├── Dockerfile           # Docker 镜像
├── .env.example         # 环境变量模板
└── README.md
```

---

## 常见问题

<details>
<summary><b>如何获取 AI 模型的 API Key？</b></summary>

- **千问**: [阿里云百炼平台](https://bailian.console.aliyun.com/)
- **GLM**: [智谱 AI 开放平台](https://open.bigmodel.cn/)
- **DeepSeek**: [DeepSeek 平台](https://platform.deepseek.com/)
</details>

<details>
<summary><b>审核失败怎么办？</b></summary>

1. 检查 API Key 是否正确
2. 查看容器日志：`docker logs ai-code-review`
3. 确认网络连接正常
</details>

<details>
<summary><b>如何备份和恢复数据？</b></summary>

```bash
# 备份
docker exec mysql-code-review mysqldump -u root -p ai_code_review > backup.sql

# 恢复
docker exec -i mysql-code-review mysql -u root -p ai_code_review < backup.sql
```
</details>

---

## 路线图

- [ ] 支持 GitHub Webhook
- [ ] 支持更多 AI 模型
- [ ] 审核规则自定义
- [ ] 审核结果评分
- [ ] 多语言支持

---

## 贡献指南

欢迎提交 Issue 和 Pull Request！

1. Fork 本仓库
2. 创建特性分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 提交 Pull Request

---

## 许可证

本项目采用 [MIT](LICENSE) 许可证。

---

<div align="center">

  **如果这个项目对你有帮助，请给个 Star ⭐**

  Made with ❤️ by AI Code Review Team

</div>
